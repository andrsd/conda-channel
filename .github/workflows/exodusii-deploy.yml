name: "Deploy package: exodusII"

on:
  push:
    branches:
      - main
    paths:
      - recipes/exodusii/meta.yaml

jobs:
  build:
    uses: ./.github/workflows/pkg-deploy.yml
    with:
      package-name: exodusii
    secrets: inherit
