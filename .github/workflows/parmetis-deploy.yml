name: "Deploy package: ParMETIS"

on:
  push:
    branches:
      - petsc-*
    paths:
      - recipes/parmetis/meta.yaml

jobs:
  build:
    uses: ./.github/workflows/pkg-deploy.yml
    with:
      package-name: parmetis
    secrets: inherit
