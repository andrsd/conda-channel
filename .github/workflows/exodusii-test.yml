name: "Test package: exodusII"

on:
  pull_request:
    paths:
      - recipes/exodusii/meta.yaml

jobs:
  build:
    uses: ./.github/workflows/pkg-build.yml
    with:
      package-name: exodusii
      upload: false
    secrets: inherit
