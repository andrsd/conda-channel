name: "Build package: MPICH"

on:
  push:
    branches:
      - main
    paths:
      - recipes/mpich/meta.yaml

jobs:
  build:
    uses: ./.github/workflows/pkg-build.yml
    with:
      package-name: mpich
      upload: true
    secrets: inherit
