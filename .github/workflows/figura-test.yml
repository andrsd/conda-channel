name: "Test package: figura"

on:
  pull_request:
    paths:
      - recipes/figura/meta.yaml

jobs:
  build:
    uses: ./.github/workflows/pkg-build.yml
    with:
      package-name: figura
      upload: false
    secrets: inherit
