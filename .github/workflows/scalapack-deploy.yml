name: "Deploy package: ScaLAPACK"

on:
  push:
    branches:
      - main
    paths:
      - recipes/scalapack/meta.yaml

jobs:
  build:
    uses: ./.github/workflows/pkg-deploy.yml
    with:
      package-name: scalapack
    secrets: inherit
