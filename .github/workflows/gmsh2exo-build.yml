name: "Build package: gmsh2exo"

on:
  push:
    branches:
      - main
    paths:
      - recipes/gmsh2exo/meta.yaml

jobs:
  build:
    uses: ./.github/workflows/pkg-build.yml
    with:
      package-name: gmsh2exo
      upload: true
    secrets: inherit
