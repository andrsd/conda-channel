name: "Test package: ScaLAPACK"

on:
  pull_request:
    paths:
      - recipes/scalapack/meta.yaml

jobs:
  build:
    uses: ./.github/workflows/pkg-build.yml
    with:
      package-name: scalapack
      upload: false
    secrets: inherit
