name: "Deploy package: HYPRE"

on:
  push:
    branches:
      - petsc-*
    paths:
      - recipes/hypre/meta.yaml

jobs:
  build:
    uses: ./.github/workflows/pkg-deploy.yml
    with:
      package-name: hypre
    secrets: inherit
